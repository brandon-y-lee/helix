We are setting up the engineering team only. Do not build, scaffold, or implement the ecommerce platform yet.

Create an agent team with these teammates. Use the matching project subagent definitions where available, but do not rely on subagent skills frontmatter for skill loading. For each teammate, explicitly use the listed project-local skills from `.claude/skills/` when performing that teammate's work.

Team:

1. architect
   - Role: architecture, system boundaries, ADRs, sequencing, tradeoffs
   - Use skills:
     - senior-architect
     - tech-stack-evaluator

2. backend
   - Role: API, domain, data, auth, backend planning
   - Use skills:
     - senior-backend
     - tdd-guide

3. frontend
   - Role: storefront UI, admin UI, frontend performance, accessibility-sensitive frontend planning
   - Use skills:
     - senior-frontend
     - a11y-audit

4. fullstack
   - Role: cross-layer integration, contracts, frontend/backend handoffs
   - Use skills:
     - senior-fullstack
     - tdd-guide

5. qa
   - Role: test strategy, acceptance criteria, verification gates
   - Use skills:
     - senior-qa
     - tdd-guide

6. security
   - Role: auth, checkout, payment, PII, secrets, admin access, dependency risk, logging, security review
   - Use skills:
     - senior-security
     - senior-secops

7. devops
   - Role: local dev, CI/CD, release gates, deployment-neutral operations planning
   - Use skills:
     - senior-devops

8. reviewer
   - Role: independent adversarial review, correctness checks, final quality checks
   - Use skills:
     - code-reviewer
     - adversarial-reviewer

9. a11y
   - Role: accessibility audit, WCAG-sensitive flows, keyboard navigation, semantic HTML, checkout/account/admin accessibility
   - Use skills:
     - a11y-audit

Important operating rules:
- Current phase is agent/team setup only.
- Do not implement application code.
- Do not scaffold the ecommerce application.
- Require plan approval before any teammate makes file changes.
- Use the shared task list.
- Assign each teammate a clearly bounded planning or review task.
- Have teammates report findings back to the lead.
- Avoid assigning two teammates to the same file set at the same time.
- Treat auth, checkout, payment, PII, secrets, sessions, and admin permissions as security-sensitive.
- Security-sensitive areas require security and reviewer signoff before future implementation merges.
- Verification evidence is required for future implementation work: command run, result, and reason the command is sufficient.
- Keep the team small for active work. Use all teammates for setup planning, but in future implementation phases activate only the relevant 3-5 teammates.

Initial assignment:
1. architect: draft the proposed engineering team operating model and handoff flow.
2. backend: identify backend planning responsibilities and future verification gates, without implementing code.
3. frontend: identify frontend planning responsibilities and future accessibility/performance gates, without implementing code.
4. fullstack: identify integration boundaries and contract handoff rules, without implementing code.
5. qa: define test strategy and acceptance criteria structure, without implementing code.
6. security: define security review gates and sensitive-domain checklist, without implementing code.
7. devops: define local/CI/release planning responsibilities, staying cloud-neutral.
8. reviewer: adversarially review this team setup for ambiguity, missing gates, and unsafe assumptions.
9. a11y: define accessibility review gates for product, cart, checkout, account, and admin flows.

Final deliverable:
- The lead should synthesize teammate findings into `docs/agent-team/OPERATING_MANUAL.md`.
- The manual should include:
  - agent roles
  - which project-local skills each agent should use
  - when to use each agent
  - handoff rules
  - file-conflict rules
  - verification gates
  - security review gates
  - accessibility review gates
  - tmux startup and recovery instructions
  - rules for when not to use the whole team